import { themes } from '@oceanblue/design-tokens/build/js/design-tokens.treat.js'

const anzx = themes.anzx
const anzxdark = themes.anzxdark
const product = themes.product
const horizon = themes.horizon

export {
  anzx,
  anzxdark,
  product,
  horizon
}